<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！

    $input_line = trim(fgets(STDIN));
    for ($i=0; $i<$input_line; $i++) {
        $input = trim(fgets(STDIN));
        $a = strlen($input);
        echo "$a\n";
    }

?>
