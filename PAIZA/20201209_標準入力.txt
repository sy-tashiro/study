<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！

    $input_line = trim(fgets(STDIN));
    for ($i=0; $i<$input_line; $i++) {
        $input = fgets(STDIN);
        $tmp = explode(' ', $input);
        $tmp[1] = $tmp[1] + 1;
        echo "$tmp[0] $tmp[1]\n";
    }
?>
