■CDN
web

■jqueryリファレンス
でjqueryの種類を確認することができる。

■DOM
DOMの情報の読み取りが重要
DOMの操作をしてWebページに反映させる。



■javascriptからDOMにアクセスして、そこからHTMLにアクセスすると、DOMの内容が反映されます。



■$(fuction() {
	
});

$の部分はjqeuryを読み込んだ時に使う部分。
ページを読み込み完了し、DOMの構築が完了した時点でfunction()内が実行されます。


■("#praice").val();
値を得ることができる。
valに値を得ると変更することもできる。

■命令を書き連ねることをメソッドチェーン

■文字列タス数字だと文字列として計算されてしまう。

■jqueryでとった値は文字列として扱われてしまうため、*1をして数字として扱えるようにする。

■■■ボタンに機能を追加する■■■
